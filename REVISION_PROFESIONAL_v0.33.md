# Revisión profesional integral — versión 0.33

## Correcciones principales

1. Portada PDF:
   - se eliminaron metadatos duplicados en `index.qmd`;
   - la portada PDF se reforzó en `include-before-body`.

2. Contenido del PDF:
   - se compactó y se configuró con `toc-depth: 1`;
   - se añadieron ajustes específicos para el inicio del contenido.

3. Notación:
   - se corrigió el conflicto entre `\beta` escalar y `\boldsymbol{\beta}` vectorial.

4. Encabezados superiores:
   - el PDF ahora muestra capítulo/sección y número de página mediante `scrlayer-scrpage`.

5. Ilustraciones:
   - se añadieron figuras nuevas para K-NN, árboles, Random Forest, SVM y redes neuronales.

6. Redes neuronales:
   - se incorporaron figuras de neurona natural vs. artificial y de arquitectura multicapa.

7. Referencias:
   - se ampliaron citas dentro del texto y bibliografía formal de capítulos 12 a 16.
